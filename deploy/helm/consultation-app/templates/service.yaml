apiVersion: v1
kind: Service
metadata:
  name: {{ include "boussole.fullname" . }}
  labels:
    {{- include "boussole.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "boussole.selectorLabels" . | nindent 4 }}
